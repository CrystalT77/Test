这是一个有关于Git使用的测试记录文件。。。。。。

一些遇到的错误记录：
（1）在执行“$ git remote add origin git@github.com:CrystalT77/Test.git”语句时，出现“fatal: Not a git repository (or any of the parent directories): .git”错误提示。
（2）在“$ git add README”时，出现“fatal: pathspec 'README' did not match any files”提示。

对应的原因及解决：
（1）使用“git init”；
（2）因为建立的Repository是个空的，没有相应的文件夹。得先执行一个“$ touch README”。
